services:
  vc:
    build: vc
    container_name: vc_service
    ports:
      - "8000:8000"
    depends_on:
      - vc_db
    environment:
      DATABASE_URL: ${DATABASE_URL}
      AC_PRIVATE_KEY: ${AC_PRIVATE_KEY}
      AC_PUBLIC_KEY: ${AC_PUBLIC_KEY}

    volumes:
      - "./vc/alembic/versions:/app/alembic/versions"
    restart: unless-stopped

  ac_db:
    image: postgres:16
    container_name: ac_postgres
    environment:
      POSTGRES_DB: vds
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5435:5432"
    volumes:
      - pgdata_ac:/var/lib/postgresql/data
    restart: unless-stopped

  vc_db:
    image: postgres:16
    container_name: vc_postgres
    environment:
      POSTGRES_DB: vds
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5436:5432"
    volumes:
      - pgdata_vc:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata_ac:
  pgdata_vc:
