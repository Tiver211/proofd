[project]
name = "authority-center"
version = "0.1.0"
description = "authority center exapmle for proofd system"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "sqlalchemy",
    "sqlmodel",
    "cryptography",
    "uvicorn",
    "alembic",
    "asyncpg",
    "psycopg2-binary"
]
